# Employee-Record-System-output

Two files employee_record_system.ipynb and sample in a single folder while running if the program shows module error you need to install modules

pip install openpyxl
pip install tkinter
